actor: SmokeTester
modules:
  enabled:
    - Asserts
    # Schema comes from the consolidated Install migration configured in
    # codeception.yml — no per-suite migration deltas needed.
    - \craft\test\Craft
    - \Helper\Integration
