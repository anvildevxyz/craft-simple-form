{#
  Default Simple Form theme — the captcha slot (#137).

  `captcha` is pre-rendered Markup (the selected provider's widget), or '' when
  captcha is disabled. A theme may move it or suppress it with an empty override.

  Variables:
    captcha   Markup|'' — the provider widget
-#}
{{- captcha -}}
